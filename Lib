import java.util.List;

public class Library {
	
	// Set up composition with users and stories
	private List<User> users; // store each user information
	private List<Story> stories; // store each story 
	
	public Library(List<User> user, List<Story> story) {
		users = user;
		stories = story;
	}
	
	public List<Story> searchStory(String title){
		// get title searched for and filter so its only lower case and remove spaces
		// search database for title using binary search / sequential search
		// return story object
		return stories; // at some index (individual)
	} // end searchStory
	
	public List<Story> searchByFilter(String keyword){
		// get keyword (genre, culture, tags, ageGroup)
		return stories; // list of stories that fall into this group
		// in database, need to hold tags for stories
	} // end searchByFilter
	
	public void saveToBookshelf(String title) {
		// add to bookshelf in user class
	
	} // end saveToBookshelf
	
	public void removeFromBookshelf(String title) {
		// remove from linked list bookshelf in user class
	} // end removeFromBookshelf
	
	public void organizeBookshelf(String keyword) {
		// click organization method:
			// genre, culture, rating, author
	} // end oragnizeBookshelf
	
	public String[] displayTopFiveStories() {
		// create a list and store the top five rated stories in them, and return the list
		String[] topFive = new String[5];
		// do stuff
		return topFive;
	} // end displayTopFiveStories
	
	public void renameBookshelf(String currentName, String newName) {
		// currentName = temp; newName = currentName; temp = currentName;
	} // end renameBookshelf
	
} // end Lib
